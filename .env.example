# International Shibari Events — SPA environment
#
# The v1 static site has no runtime environment variables. This file exists
# so the convention is in place for future needs (build-time flags, analytics
# opt-outs, etc.).
#
# For Worker secrets (SIGNING_KEY, RESEND_API_KEY, etc.), see
# worker/.dev.vars.example.
